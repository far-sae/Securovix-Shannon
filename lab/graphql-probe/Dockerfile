# In-house deterministic GraphQL introspection prober image for broker e2e tests (no GPL deps).
FROM python:3.12-slim
COPY graphql_probe.py /usr/local/bin/graphql-probe
RUN chmod +x /usr/local/bin/graphql-probe
ENTRYPOINT []
