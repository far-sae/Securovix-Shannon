# In-house deterministic insecure-deserialization prober image (no GPL deps).
FROM python:3.12-slim
COPY deser_probe.py /usr/local/bin/deser-probe
RUN chmod +x /usr/local/bin/deser-probe
ENTRYPOINT []
