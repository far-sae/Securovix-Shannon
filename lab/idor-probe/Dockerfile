# In-house deterministic IDOR/BOLA prober image for broker e2e tests (no GPL deps).
FROM python:3.12-slim
COPY idor_probe.py /usr/local/bin/idor-probe
RUN chmod +x /usr/local/bin/idor-probe
ENTRYPOINT []
